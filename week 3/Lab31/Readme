Method	| Library|	Code|	Result
------ | -------- | ---- | ------------- |
Dummy Encoder|	pandas	|z = pd.get_dummies(df.Profession, prefix='Profession')|	5 columns created
LabelEncoder	|sklearn|	"from sklearn.preprocessing import LabelEncoder
label=LabelEncoder()
df2= df[""Profession""] = label.fit_transform(df[""Profession""])"	| column data encoded within the dataframe
OrdinalEncoder|	sklearn|	"from sklearn.preprocessing import OrdinalEncoder
enc = OrdinalEncoder()
enc.fit(df[[""Risk""]])
df[[""Risk""]] = enc.transform(df[[""Risk""]])"|	column data encoded within the dataframe
